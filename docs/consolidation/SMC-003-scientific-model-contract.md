# SMC-003 — Scientific Model Contract

Every ScientificModel must expose identity, scientific definition, applicability, assumptions, limitations, inputs, outputs, units/dimensions, provenance, verification and validation references.

Domain modules consume this contract; they do not redefine Scientific Core semantics.
